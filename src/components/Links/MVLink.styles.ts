import { NavLink } from 'react-router'
import styled from 'styled-components'

export const StyledMVLink = styled(NavLink)`
  display: inline-flex;
  align-items: center;
  justify-content: center;
`

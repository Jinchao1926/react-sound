import styled from 'styled-components'

import { PlayControl as PlayControlComponent } from '@/components/Shared/Playbar'

export const PlayControl = styled(PlayControlComponent)`
  display: flex;
  align-items: center;
  gap: 2px;
  padding-left: 13px;
`
